return {
	buttons = require("binds.buttons"),
	misc = require("binds.misc"),
	layout = require("binds.layout"),
	clients = require("binds.clients"),
	window = require("binds.window"),
	tags = require("binds.tags"),
	hints = require("binds.hints")
}
